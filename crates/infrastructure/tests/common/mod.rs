pub mod fixtures;

// Re-export all repository implementations for easy access in tests
pub use infrastructure::repositories::OrganizationRepositoryImpl;

// Add more repositories here as needed:
// pub use infrastructure::repositories::ContactRepositoryImpl;
// pub use infrastructure::repositories::ProductRepositoryImpl;
